// Este archivo está DEPRECADO - la aplicación ahora usa Next.js
// Ver archivo /app/page.tsx en su lugar

// Las siguientes líneas son solo para referencia histórica
// Ya no se ejecutan cuando se usa Next.js

/*
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.tsx'

console.warn('⚠️ main.tsx está deprecado. La aplicación ahora usa Next.js. Ver /app/page.tsx')

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
*/

export {}
